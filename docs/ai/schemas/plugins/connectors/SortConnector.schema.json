{
  "$schema" : "http://json-schema.org/draft-07/schema#",
  "title" : "SortConnector",
  "type" : "object",
  "description" : "Sorts all rows",
  "properties" : {
    "pluginId" : {
      "type" : "string",
      "description" : "Must equal SortConnector"
    },
    "sourceConnectorName" : {
      "type" : "string",
      "title" : "Source connector",
      "description" : "Upstream connector this transform reads from (if applicable)"
    },
    "cacheOnDisk" : {
      "type" : "boolean",
      "title" : "Cache data on disk?",
      "description" : "Store connector output as Hop binary rows under the server data path. Reloads only when the connector changes or a full presentation refresh is requested."
    },
    "columns" : {
      "type" : "array",
      "title" : "Sort columns",
      "description" : "Columns used as sort keys (list editor; annotation type ignored for List fields)"
    },
    "sortMethods" : {
      "type" : "array",
      "title" : "Sort methods",
      "description" : "One method per sort column (same list size; List fields always render as LIST)"
    }
  }
}