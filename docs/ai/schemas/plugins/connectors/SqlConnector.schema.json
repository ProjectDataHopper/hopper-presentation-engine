{
  "$schema" : "http://json-schema.org/draft-07/schema#",
  "title" : "SqlConnector",
  "type" : "object",
  "description" : "Reads data from a relational database using a SQL query",
  "properties" : {
    "pluginId" : {
      "type" : "string",
      "description" : "Must equal SqlConnector"
    },
    "sourceConnectorName" : {
      "type" : "string",
      "title" : "Source connector",
      "description" : "Upstream connector this transform reads from (if applicable)"
    },
    "cacheOnDisk" : {
      "type" : "boolean",
      "title" : "Cache data on disk?",
      "description" : "Store connector output as Hop binary rows under the server data path. Reloads only when the connector changes or a full presentation refresh is requested."
    },
    "databaseConnectionName" : {
      "type" : "string",
      "title" : "Database connection",
      "description" : "Name of a hopper-database-connection metadata element"
    },
    "sql" : {
      "type" : "string",
      "title" : "SQL",
      "description" : "SQL query executed against the selected database connection"
    }
  }
}