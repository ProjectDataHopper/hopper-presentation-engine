<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.hopper</groupId>
  <artifactId>hopper-presentation-engine</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Hopper Presentation Engine</name>
  <description>
    Metadata-driven presentation engine for Data Hopper — connectors, SVG rendering,
    and a REST canvas editor for reports and interactive dashboards.
  </description>
  <url>https://github.com/ProjectDataHopper/hopper-presentation-engine</url>

  <modules>
    <module>hopper-presentation-core</module>
    <module>hopper-presentation-rest</module>
  </modules>

  <licenses>
    <license>
      <name>Apache License, version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <hop.version>2.18.1</hop.version>
    <jandex.version>3.5.3</jandex.version>
    <jersey.version>3.1.9</jersey.version>
    <jetty.version>11.0.24</jetty.version>
    <jackson.version>2.18.2</jackson.version>
    <lombok.version>1.18.36</lombok.version>
    <batik.version>1.19</batik.version>
    <junit.version>5.11.4</junit.version>

    <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
    <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
    <maven-source-plugin.version>3.3.1</maven-source-plugin.version>
    <jandex-maven-plugin.version>3.5.3</jandex-maven-plugin.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.hopper</groupId>
        <artifactId>hopper-presentation-core</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
          <configuration>
            <release>${maven.compiler.release}</release>
            <annotationProcessorPaths>
              <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
              </path>
            </annotationProcessorPaths>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven-surefire-plugin.version}</version>
          <configuration>
            <forkCount>1</forkCount>
            <reuseForks>true</reuseForks>
            <parallel>none</parallel>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
